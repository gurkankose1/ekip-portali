
import React from 'react';

// This component is no longer used as notifications and tasks are handled within the Chat view.
// It is kept as an empty file to prevent import errors in case of future use.

const GlobalWidget: React.FC = () => {
  return null;
};

export { GlobalWidget };
